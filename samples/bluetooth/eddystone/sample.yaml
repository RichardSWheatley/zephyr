sample:
  name: Bluetooth Eddystone
tests:
  sample.bluetooth.eddystone.console:
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Configuration mode: waiting connections"
    platform_allow:
      - apollo510L_eb
  sample.bluetooth.eddystone.bluetooth_harness:
    harness: bluetooth
    platform_allow:
      - qemu_cortex_m3
      - qemu_x86
    tags: bluetooth
    integration_platforms:
      - qemu_cortex_m3
